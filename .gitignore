# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
dist/
build/

# Environment files — NEVER commit these
.env
.env.local
.env.*.local
*.env

# Prisma
apps/api/prisma/migrations/

# Misc
.DS_Store
*.log
.turbo/
.cache/
*.tsbuildinfo

# Agent reports — local only, never commit
reports/

# Claude Code local config — machine-specific, never commit
.claude/

# Session notes and project spec — local planning docs, never commit
NEXT_SESSION.md
PROJECT_SPEC.md
